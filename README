Setup
-----
Upload 'php_reqs' folder directly outside public_html
Upload 'public' folder contents to a root directory. If you are putting it in a subdomain, make sure the subdomain's files are not located inside public_html. Otherwise change the require path in the php files.
Change d